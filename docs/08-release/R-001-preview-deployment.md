# R-001 Preview Deployment and Release Gate

## Status

PREVIEW READY — HOSTED WORKFLOW NOT YET ENABLED

Owner decision: R-001 corrected Samawah Preview is accepted in this state. Full hosted workflow smoke remains blocked by F-001 UI scope: the hosted production-mode build disables route fixture actors and the sign-in page is a placeholder for a later Supabase Auth wiring slice. No new feature work was started to bypass that boundary.

This release evidence must not be treated as UAT-ready, and it does not authorize F-002.

## PR URL

- PR #1: https://github.com/samawah-media/sherk/pull/1
- Status: merged by owner-approved remote workflow deviation before this recovery.
- Feature branch: `feat/f001a-secure-client-foundation`

## Preview URL

- Preview deployment: https://sherk-f001-preview-ejin6wyo2-samawahs-projects.vercel.app
- Stable Vercel alias: https://sherk-f001-preview-samawahpod-2242-samawahs-projects.vercel.app
- Vercel deployment id: `dpl_2kKpGMeVJVocrBeSYGK2WbbLHRUn`
- Vercel account: `samawahpod-2242`
- Vercel team/scope: `samawahs-projects`
- Target: `preview`
- Protection: Vercel Authentication remains enabled.
- Alias observed: `sherk-f001-preview-samawahpod-2242-samawahs-projects.vercel.app`
- Production root alias `sherk-f001-preview.vercel.app`: not assigned after recovery.

## Staging Environment

- Supabase project name: `samawah-f001-preview-staging`
- Supabase project ref: `kbntdgjzsfuqqdythmrn`
- Region: `eu-west-1`
- Status: `ACTIVE_HEALTHY`
- Data policy: dummy staging data only; no real customer data.

## Commit Deployed

- Deployed source commit at the Samawah Preview recovery point: `628455dddf8b4fa1e06272121db9cf99aeb58372`
- The final deployment was created from a temporary `git archive` of that clean HEAD to avoid uploading untracked local build artifacts.

## Migration Status

Applied to staging with Supabase CLI:

- `202606230001`
- `202606230002`
- `20260624083439`
- `20260624095927`

RLS verification previously confirmed enabled policies on the F-001 tenant/client/membership/invitation/audit tables.

## Seed Status

Seed applied to staging after aligning with Supabase Auth generated columns.

Seed row-count evidence:

- `auth.users`: 8
- `auth.identities`: 8
- `tenants`: 1
- `clients`: 3
- `tenant_memberships`: 8
- `client_memberships`: 3
- `role_assignments`: 8
- `permission_references`: 11
- `invitations`: 2
- `audit_events`: 1

Seed names:

- Tenant: `سماوة`
- Clients: `عميل تجريبي ألف`, `عميل تجريبي باء`, `عميل تجريبي جيم`

## Smoke Test Results

Hosted protected smoke via `vercel curl` passed 9/9 checks:

- sign-in surface
- create client denied without hosted auth
- internal invite denied without hosted auth
- client portal denied without hosted auth
- cross-tenant denial
- cross-client denial
- fixture actor disabled in hosted preview
- member lifecycle surface
- invitation accepted state

Full requested workflow smoke is not completed:

- Actual login is not wired in the F-001 hosted UI.
- Create Client form is a surface only in the hosted production-mode build.
- Internal/client invitation submission is not wired in the hosted UI.
- Role change, disablement, and audit flows are represented by accepted command tests and static surfaces, not hosted UI actions.

This was not changed during R-001 because product code work is closed and adding those flows would be new feature scope.

## Security Findings

- No Production Supabase project was connected.
- Vercel Production environment variables are empty.
- Preview environment variables are scoped to Preview.
- `SUPABASE_SERVICE_ROLE_KEY` is configured as a sensitive Preview variable.
- Browser HTML check did not expose `SUPABASE_SERVICE_ROLE_KEY` or `service_role`.
- Vercel Authentication remains enabled for the Preview deployment.
- A Vercel automation bypass token was generated by `vercel curl` for protected smoke access; the token value was not printed or stored in Git.

## Skipped Test Justification

Full E2E previously had 52 passed / 2 skipped.

Skipped tests:

- `[desktop-chromium] tests/e2e/accessibility/rtl-mobile.spec.ts:22 › client portal mobile navigation keeps primary actions visible`
- `[rtl-arabic] tests/e2e/accessibility/rtl-mobile.spec.ts:22 › client portal mobile navigation keeps primary actions visible`

Reason:

- Conditional `test.skip(!isMobile, "mobile assertion runs in the mobile Playwright project")`

Acceptance criticality:

- Not acceptance-critical as skipped instances because the mobile assertion runs and passes in `mobile-chromium`.

## Recovery Notes

The first CLI deployment unexpectedly created Vercel target `production` even without `--prod`.

Recovery actions:

- Recorded the wrong deployment `dpl_BQmCXn3CdSX9UuHPNFcUdqMMQEcp`.
- Deleted it.
- Verified Production env vars were empty.
- Verified no Production Supabase connection.
- Repeated direct CLI attempts with `--target=preview`; Vercel CLI still produced `target=production`.
- Deleted each wrong deployment.
- Created a temporary source deployment and used Vercel REST API redeploy without `target`, which Vercel represents as Preview (`target=null` in API, `target=preview` in CLI inspect).
- Deleted the temporary Production source deployment.
- Created the final clean Preview with `vercel deploy --target=preview --archive=tgz` from a temporary `git archive` working directory.
- Deleted the earlier recovery Preview `dpl_4KJRx9KiTgY7TaBao1EfbKZZQmcc`; `dpl_2NoZUyPzNLK2cgAoAiUtPMtPZ1aX` was then identified as being under the wrong Omar Vercel account and is no longer the accepted R-001 Preview.
- Owner identified the previous Preview was deployed under the wrong Vercel account (`omar-x-arts-projects`).
- Re-authenticated Vercel CLI as `samawahpod-2242`, linked `samawahs-projects/sherk-f001-preview`, and configured Preview-only staging environment variables.
- The first Samawah deployment was incorrectly assigned `target=production` by Vercel because it was the first deployment for the new project; Production env vars were empty. It was deleted: `dpl_2EncQxyacuucSyKdFFaegrNE3Lsp`.
- A same-artifact API Preview without env metadata was also deleted: `dpl_EdCETGwRaH32RtuD8y3Mkq5xrw3M`.
- The remaining Samawah deployment is the verified Preview: `dpl_2kKpGMeVJVocrBeSYGK2WbbLHRUn`.

## Known Residuals

- GitHub/Vercel Git integration remains deferred technical debt.
- The earlier Omar-account Preview could not be deleted after CLI authentication switched to `samawahpod-2242`; deleting it is deferred and non-blocking for R-001.
- Hosted full workflow smoke cannot pass until a later owner-approved scope wires real Supabase Auth and server actions into the UI, or an approved staging-only smoke harness is added.
- Local `vercel build --target=preview` on Windows failed with an `EPERM` symlink error, so the accepted Samawah Preview uses Vercel remote build from a clean `git archive` instead.
- `APP_ENV` is `development` because current schema allows `local | development | test`; this does not indicate Production connectivity.

## Rollback Procedure

1. Delete the Preview deployment by id with `vercel remove <deployment-id> --yes --scope=samawahs-projects`.
2. Remove Preview env vars only if the project is being retired.
3. Keep Supabase staging project for audit unless owner approves deletion.
4. Do not merge or push to `main` as part of rollback.

## Owner Acceptance

Accepted for R-001 closeout as:

- `PREVIEW READY — HOSTED WORKFLOW NOT YET ENABLED`

Next workflow enablement requires a separate owner-approved F-001B scope and must not use fixture bypasses to make UAT appear successful.
