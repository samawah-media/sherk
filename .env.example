# Development placeholders only. Do not store real secrets in this file.

NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=replace-with-local-publishable-key

# Server/admin scripts only. Never expose this through NEXT_PUBLIC_*.
SUPABASE_SERVICE_ROLE_KEY=replace-with-local-service-role-key-never-expose

# local | development | test | preview | staging | production
APP_ENV=local
